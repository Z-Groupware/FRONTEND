/**
 * 초대 목록의 열 폭 — **행(`InviteRow`)과 열 머리(`InviteColumnHead`)가 같은 값을 본다.**
 *
 * ⚠️ 두 파일에 숫자를 따로 적어 두면 한쪽만 고쳐져 열이 통째로 밀린다. 실제로 부서 칸을
 *    넓히면서 그 일이 났다 — 셀렉트는 112인데 감싼 칸은 96이라 머리글과 세로축이 어긋났다.
 * ⚠️ 선택 칸 폭(`SELECT`)은 다섯 글자(`프론트엔드` 61px) + 꺽쇠(14) + 사이(4) + 좌우 여백(16)
 *    = 95px에 맞춘 **타이트한** 값이다. 줄이면 다섯 글자가 잘리고, 늘리면 글자 옆이 빈다.
 */
export const INVITE_COLUMN = {
  /** 번호 */
  INDEX: "w-5",
  /** 이름 — 세 글자가 대부분이라 넓으면 양옆이 빈다 */
  NAME: "w-[68px]",
  /** 이메일 — 남는 자리는 오류 문구 칸이 먹는다 */
  EMAIL: "w-[224px]",
  /**
   * 오류가 뜬 줄의 이메일 칸 — **48px을 문구에게 내준다.**
   *
   * ⚠️ 평소 문구 자리는 76px뿐이라 한글 문장이 통째로 잘린다(`직급을 골...`).
   *    그렇다고 이메일을 늘 좁히면 `sugskd1010@naver.com`(187px)이 안 들어간다 —
   *    그래서 **문구가 있는 줄에서만** 좁힌다. 오른쪽 선택 칸들은 그대로 서 있는다.
   */
  EMAIL_WITH_MESSAGE: "w-[176px]",
  /** 오류 문구 — 남는 폭 전부 */
  MESSAGE: "flex-1",
  /** 부서·역할·직급 */
  SELECT: "w-[100px]",
  /** Admin */
  ADMIN: "w-[44px]",
  /** 줄 빼기(X) */
  REMOVE: "size-6",
} as const;

/** `OptionSelect`는 클래스가 아니라 px를 받는다 — `INVITE_COLUMN.SELECT`와 같은 값이어야 한다 */
export const INVITE_SELECT_WIDTH = 100;
