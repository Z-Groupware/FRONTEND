import { PLAN, type Plan } from "./types";

/**
 * 요금제.
 *
 * ⚠️ **유료 하나뿐이다**(2026-08-04 확정). 무료 요금제를 두면 화면마다 "무료면 이렇게,
 *    유료면 저렇게" 분기가 두 벌씩 생기는데, 그 분기가 주는 값에 비해 유지 비용이 컸다.
 *    **결제 전·해지 후는 플랜이 아니라 구독 상태**로 다룬다(`SUBSCRIPTION_STATUS`).
 * ⚠️ **목이다.** 가격이 확정되지 않았다(DECISIONS §미결정: 결제 실연동 여부).
 *    확정되면 BE에서 내려받고 이 파일은 지운다 — 컴포넌트는 `Plan`만 보므로 고칠 게 없다.
 * ⚠️ `constants/`에 두지 않는다. 확정된 도메인 상수가 아니라 아직 바뀌는 값이다.
 * ⚠️ **"AI 요약"이라고 쓰지 않는다** — 명세상 AI의 산출물은 요약문이 아니라 **액션 할당**이다.
 *    STT(자막)는 브라우저 기능이라 AI로 표기하지 않는다(CLAUDE.md §AI 기능).
 * ⚠️ **인원은 과금 단위가 아니다**(2026-08-04). 좌석 과금을 걷어내면서 결제 화면의 슬라이더도
 *    같이 사라졌다 — 금액을 움직이는 건 AI 토큰·저장 공간뿐이다.
 */

export const PLANS: readonly Plan[] = [
  {
    code: PLAN.TEAM,
    name: "Team",
    /*
      ⚠️ **VAT 별도라고 밝힌다.** 경쟁사(카카오워크·네이버웍스·잔디)가 전부 VAT 별도로
         표기해서, 우리만 안 적으면 비교할 때 우리가 10% 비싼 것처럼 읽힌다.
    */
    /*
      ⚠️ **금액·포함량은 여기 적지 않는다.** 그 값들은 BE 설정(`BillingConfig`)에서 오고,
         화면이 그때그때 읽는다 — 실측 전 가정값이라 바뀔 것이 확정돼 있다(팀 확정).
         여기엔 **바뀌지 않는 기능 목록**만 둔다.
    */
    /*
      ⚠️ **묶어 적지 않는다.** 전에는 `실시간 자막 · AI 액션 분배`처럼 두 가지를 한 줄에 넣어
         네 줄로 보였다 — 파는 게 적어 보여 결제를 망설이게 된다. 같은 범위를 한 줄에
         하나씩 적으면 실제 크기가 드러난다. **`PlanSummaryCard`가 이 목록을 그대로 쓴다** —
         `/plans`와 결제 화면이 다른 말을 하지 않게 한 벌만 둔다.
      ⚠️ **없는 것을 적지 않는다.** 자막은 브라우저 기능이라 AI로 표기하지 않고,
         인수인계 취합은 규칙 기반이라 AI가 아니다(CLAUDE.md §AI 기능).
    */
    /*
      ⚠️ **여덟 개**다(2026-08-16 — '프로젝트 자동 매칭'을 뺐다. 팀 확정: 그런 기능은 없다).
         3열 격자(`plan-summary-card.tsx`)의 마지막 줄은 두 칸만 찬다 — 열 폭이 `max-content`라
         격자 전체 폭은 안 변하고, 마지막 줄 오른쪽에 3열 폭만큼 빈다. 정상이다.
      ⚠️ 개수를 채우려고 없는 기능을 적지 않는다. 머리말 개수는 `CURRENT_PLAN.features.length`가
         읽으므로 여기만 고치면 따라온다.
      ⚠️ 한 줄에 세 개가 들어가야 하므로 문구를 짧게 유지한다 —
         `회의 · 자막 · 액션 통합 검색`은 칸을 넘겨 `회의 · 액션 통합 검색`으로 줄였다.
    */
    features: [
      "실시간 자막 기록",
      "AI 회의 요약 · 결정 추출",
      "AI 액션 분배",
      "회의 · 액션 통합 검색",
      "인수인계 문서 자동 취합",
      "조직도 · 권한 관리",
      "회의실 예약 · 보드",
      "실시간 알림",
    ],
    /*
      ⚠️ `isRecommended`를 켜지 않는다. 파는 게 하나뿐이라 "가장 많이 선택합니다"는
         비교 대상이 없는 말이 된다 — 플랜이 다시 늘면 그때 켠다.
    */
  },
];

/**
 * 지금 파는 플랜.
 *
 * ⚠️ 하나뿐이라 고를 일이 없다 — 화면은 "고르기"가 아니라 **"이걸 삽니다"** 로 만든다.
 *    없으면 화면이 그릴 게 없으므로 `undefined` 가능성을 감춘다.
 */
export const CURRENT_PLAN: Plan = PLANS[0] as Plan;

/**
 * 아예 세지 않는 것들 — 포함량과 **성격이 다르다.**
 *
 * ⚠️ 화면 문구가 아니라 **플랜 데이터**라 여기 둔다. 전에는 카드 컴포넌트 안에 박혀 있어서
 *    테스트가 잡지 못했다 — "인원 200명" 같은 말이 돌아와도 아무도 못 막는다.
 * ⚠️ 보관을 **"무제한"이라고 적지 않는다.** 자막·요약도 저장 공간에 포함돼 계산되므로
 *    (`buildUsage`) 무제한이 아니다 — 기한이 없다는 것과 용량을 안 쓴다는 건 다른 말이다.
 */
export const PLAN_UNLIMITED = ["인원 제한 없음", "회의 횟수 제한 없음", "보관 기한 없음"] as const;
