/**
 * 결제 화면 폭 — **온보딩 4단계와 구독 재개 화면이 같이 쓴다.**
 *
 * ⚠️ 두 화면에 `max-w-[1120px]`를 각자 적어 두면 한쪽만 넓힐 때 결제 칸 크기가 달라진다.
 *    1120px은 카드 두 장이 여백을 넉넉히 쓰면서 한 화면에 들어오는 폭이다.
 */
export const PAYMENT_WIDTH = "max-w-[1120px]";
