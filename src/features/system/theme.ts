/**
 * 운영자 화면의 밝기를 담는 쿠키 이름.
 *
 * ⚠️ 서버(레이아웃)와 클라이언트(토글)가 **같은 이름**을 봐야 한다. 한쪽만 고치면 토글은
 *    먹는데 새로고침하면 되돌아간다 — 그래서 문자열을 양쪽에 적지 않고 여기 한 곳에 둔다.
 * ⚠️ 값은 `light`일 때만 의미가 있다. 없거나 다른 값이면 다크다(§셸: 여기는 다크가 기본).
 */
export const SYSTEM_THEME_COOKIE = "z-system-theme";
