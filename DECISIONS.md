# DECISIONS.md — Z 프론트엔드 결정 기록

> 팀이 확정한 것과 아직 못 정한 것. **여기가 단일 기준**이다.
> 규칙 문서(`CLAUDE.md` · `docs/CONVENTIONS.md`)는 이 결정을 반영한 결과물이다.
> 결정이 바뀌면 **여기를 먼저 고치고** 규칙 문서를 따라 고친다.

---

## ✅ 확정

### 스택 · 환경

| 항목       | 결정                                                             |
| ---------- | ---------------------------------------------------------------- |
| 프레임워크 | **Next.js (App Router)**                                         |
| 언어       | TypeScript **strict** + `noUncheckedIndexedAccess`               |
| 스타일     | **Tailwind + shadcn/ui**                                         |
| 런타임     | Node 22 · npm                                                    |
| 뷰포트     | **1440 기준 설계 + 반응형 여지 확보** (아래 참고)                |
| 데이터     | Server Component 조회 + Server Action 변경 + BFF + httpOnly 쿠키 |

### 분업 — 화면 축 3분할

> 기능 축(캡처/실시간/뷰)으로 나누면 **캡처 화면 하나를 둘이 만지게 되어** 충돌한다. 화면 축으로 나눈다.

| 담당           | 범위                                                                                  | 화면 수  |
| -------------- | ------------------------------------------------------------------------------------- | -------- |
| **① 워크벤치** | `/app/meeting/*`(캡처·AI검토 포함) · `/app/rooms` · 캘린더                            | ~12      |
| **② 업무**     | `/app/board` · 액션 · 프로젝트 · 인수인계                                             | ~14      |
| **③ 조직·셸**  | `(role)` 대시보드 4종 · 사원/회의실 관리 · 구독 · **공유 셸·디자인 시스템 단독 소유** | ~15 + 셸 |

- **캡처(녹음) + STT(자막)는 한 화면** → 반드시 ①에게. 나누지 않는다.
- **공유 셸**(`app/layout.tsx` · 사이드바 · `globals.css` · `middleware.ts` · providers) = **③ 단독 소유 + 별도 PR로만.**
- Public · 온보딩은 초반에 나눠 빠르게 끝내거나 ③이 흡수.
- ⏳ **누가 ①②③을 맡을지는 미정** (아래 미결정 참조)

### 라우트 그룹

```
app/
├─ (public)/      /  /login  /register  /plans
├─ (onboarding)/  /onboarding/*
├─ (role)/        owner | manage | team | my    ← 셸 공유, 네비만 역할별
├─ (shared)/      /billing  /members            ← **권한**으로 들어오는 공용 화면
├─ (app)/         /app/*                        ← 공용 워크벤치(권한 차등)
└─ (system)/      /system/*                     ← 목업(더미)
```

**기업 코드는 경로에 붙지 않는다** — 세션이 기업을 안다(아래 §멀티테넌시).

- `(role)` 4개는 **레이아웃 1개 + 역할별 네비 구성**. 4벌 복붙 금지.
- `/owner`·`/manage`는 사원관리 권한이 거의 같다 → **공용 컴포넌트 + 권한 prop.**
- `/manage`는 **Admin 겸직자에게 보이는 화면**이다(2026-08-03). 역할이 아니라 겸직이라
  네비 구성은 `role` 하나로 못 정한다 — `role` + `isAdmin` 둘을 같이 본다.
- `/app/*`은 라우트를 쪼개지 말고 **컴포넌트 레벨 가드.**

#### 관리 기능은 `/manage/*` 하나로 (2026-08-05 확정)

**구독·결제·사원 관리·용량은 `/owner/*`가 아니라 `/manage/*`다.** OWNER와 Admin 겸직자가
같이 쓰는 화면이라 역할 경로에 두면 겸직자에게 **주소가 거짓말을 한다**. 두 곳에 나눠 두면
같은 화면이 두 벌이 된다.

- 가드는 `역할 === "admin"`이 아니라 **`role === "owner" || isAdmin`** 이다.
- 판정은 `canManageBilling(actor)`·`canManageMembers(actor)` 한 곳(`lib/permission.ts`).
- **"관리자 권한 부여" 토글은 Owner·Admin 둘 다** 조작한다(팀 합의). Admin인 인사팀장이
  다른 팀장에게 Admin을 주는 경우가 실제로 있다.
- ⚠️ 단 **`/owner/leader-handovers`는 Owner 전용**이다. Admin도 결국 팀장급인데, 팀장급이
  다른 팀장의 오프보딩 인수인계를 승인하는 건 위계상 성립하지 않는다.
- 경로가 공용이라고 아무나 들어와도 된다는 뜻이 아니다 — **서버에서 재검사한다**(§권한).

> 전에는 `(shared)/billing`·`(shared)/members`로 뺐다(#67). 같은 문제를 푼 것이지만
> 팀 URL 문서가 `/manage` 통합으로 정리했으므로 그쪽 이름을 따른다.

#### 사원 초대 — 링크가 아니라 계정 발급 (2026-08-03 확정)

**초대를 보내면 그 자리에서 계정이 만들어지고, 아이디와 첫 비밀번호가 메일로 나간다.**
받는 사람이 링크를 눌러 스스로 가입하는 방식이 **아니다.**

- 그래서 `/invite/[token]` 라우트도, 초대 링크 유효기간도 **없다**(둘 다 걷어냈다).
- ⚠️ **좌석을 소모하지 않는다**(2026-08-04). 인원은 과금 단위가 아니라서, 몇 명을 초대해도
  기본료가 그대로다 — "초대함"과 "계정 생김"이 같은 순간이라는 점만 남는다.
- 첫 비밀번호를 메일로 보내므로 **최초 로그인 시 변경을 강제할지**는 미정(§미확정).

#### 요금제 — 유료 하나뿐 (2026-08-04 확정 · **바뀔 수 있음**)

> ⚠️ **권한은 확정이지만 결제 정책은 아니다.** 가격·한도·청구 규칙은 팀 판단으로 바뀔 수 있다.
> 그래서 코드에서 **세 곳에만** 모아 뒀다 — 정책이 바뀌면 여기만 고치고 화면은 안 건드린다.
>
> | 무엇                   | 어디                                         |
> | ---------------------- | -------------------------------------------- |
> | **금액·포함량·단가**   | `billing/types.ts` `BillingConfig` ← BE 설정 |
> | 그 값을 받아오는 곳    | `billing/server.ts`                          |
> | 금액 계산(부가세·초과) | `billing/pricing.ts`                         |
> | 소진율·초과량 계산     | `billing/usage.ts`                           |
> | 파는 기능 목록         | `billing/plans.ts`                           |

**무료 요금제를 없앴다.** 무료를 두면 화면마다 "무료면 이렇게, 유료면 저렇게" 분기가 두 벌씩
생기는데, 그 분기가 주는 값보다 유지 비용이 컸다.

- 파는 건 **Team 하나**(회사당 월 정액 + VAT). **기능 잠금도 인원 제한도 없다.**
- **인원은 과금 단위가 아니다**(2026-08-04 변경). 몇 명을 초대해도 기본료는 같다.
  금액을 움직이는 건 **AI 토큰과 저장 공간** 두 축뿐이고, 포함량을 넘긴 만큼만 더해진다.
- **체험 기간 없음.** 기업 승인 → 로그인 → 온보딩 → **결제**를 마쳐야 워크스페이스가 열린다.
- ⚠️ 금액·포함량·초과 단가는 문서에 옮겨 적지 않는다. **`billing/types.ts`의 `BillingConfig`가
  정본**이고 값은 BE 설정에서 온다(`billing/server.ts`). `pricing.ts`(계산)·`usage.ts`(소진율)·
  `plans.ts`(기능 목록)는 전부 그 값에서 파생된다 — 실측 전 가정값이라 바뀔 것이 확정돼 있다.

**결제 전·해지 후는 플랜이 아니라 상태다** — `SUBSCRIPTION_STATUS`로 다룬다.

| 상태        | 뜻                        | 쓸 수 있나                                |
| ----------- | ------------------------- | ----------------------------------------- |
| `ACTIVE`    | 결제 중                   | ✓                                         |
| `CANCELING` | 해지 예약 — 이번 주기까지 | ✓ (이미 낸 기간이라 아무것도 막지 않는다) |
| `UNPAID`    | 온보딩은 끝났고 결제 전   | ✕ (결제 화면에서 막힌다)                  |
| `EXPIRED`   | 기간 끝                   | ✕ (기록은 지우지 않는다)                  |

판정은 `canUseWorkspace(status)` 한 곳에서 한다. 화면마다 상태를 비교하면 상태가 늘 때 빠뜨린다.

**온보딩 4단계 = 결제** — `/onboarding/payment`. 껍데기가 **온보딩 셸**이라 사이드바·상단바가
없다. 셸이 보이면 이미 들어온 것처럼 읽혀 "여기서 막혀 있다"가 전달되지 않는다.
**건너뛸 길을 두지 않는다** — "나중에 하기"가 있으면 조직만 만들어 놓고 안 쓰는 회사가 남는다.

**청구 시점** — 기본료는 **매월 결제일에** 자동으로 나간다. 결제 주기는 **월간 하나**다
(연간을 두면 1년치를 미리 냈는데 초과분만 매달 따로 빠져 두 경로가 갈린다).
초과분은 **그 주기에 바로 받지 않고** 다음 결제일에 기본료와 합쳐 청구한다.

⚠️ **좌석·일할계산 개념은 없어졌다**(2026-08-04). 인원이 과금 단위가 아니라서
늘리고 줄이는 순간이 없고, 따라서 일할계산할 대상도 없다.

**개정 이력**

- 2026-08-03 — Free / Team 두 플랜. Free에 인원 5명 · 캡처 월 20회 · 보관 30일 한도.
- 2026-08-04 — **Free 폐지.** 유료 하나로 가고, 결제를 온보딩 마지막 단계로 넣었다.
  옛 문서·화면에 "무료로 시작하기"가 남아 있으면 그건 이 개정 전 것이다.

**같이 걷어낸 것** — 플랜 선택 화면(`/pricing`)과 `PlanSelect`·`PlanCard`를 지웠다.
고를 게 하나뿐이라 "플랜을 선택하세요"가 선택이 아니게 됐다. 필요해지면 git 이력에서 되살린다 —
안 쓰는 화면을 남겨 두면 다음 사람이 그게 살아 있는 흐름인 줄 알고 고친다.

**손대지 않은 것** — 운영자 목업(`/system`)의 기업 목록·플랜 분포는 아직 Free 기업을 들고 있다.
목업이고 화면 재설계가 필요해 이번 정리에서 뺐다(팀 결정).

**개정 이력** — 2026-08-03 이전 문서는 링크 가입 방식으로 적혀 있었다. 화면 문구·라우트가
그 전제로 짜여 있었으므로, 옛 문서를 참고할 때 주의한다.

### 멀티테넌시 — 기업 코드는 URL에 넣지 않는다 (2026-07-30 확정)

**기업 식별은 세션(httpOnly 쿠키)이 한다.** `/acme/owner` 같은 기업 코드 세그먼트도, `?companyId=` 쿼리도 쓰지 않는다.

- 로그인 시 발급된 토큰에 `companyId`가 들어 있고, **BFF가 BE로 보낼 때 그 값을 싣는다.** 서버 컴포넌트·Server Action은 세션에서 읽는다.
- URL에 넣지 않는 이유 — **주소창 값은 사용자가 고칠 수 있다.** 결국 서버가 세션과 대조해야 하므로 URL 파라미터는 보안에 기여하지 못하면서 모든 링크·`revalidatePath`·미들웨어에 전파된다.
- **기업 코드가 등장하는 곳은 로그인 전뿐이다** — `/login`(워크스페이스 지정) · `/register`. 세션이 생긴 뒤에는 쿠키가 대신한다.
- **한 계정 = 한 기업.** 확정이다(회사가 계정을 발급하는 구조). 워크스페이스 전환·계정별 기업 목록 같은 건 만들지 않는다.
- 그래서 라우트 그룹 `(role)`·`(app)`·`(system)`은 **접두사 없이** 그대로 간다.
- ⚠️ **"httpOnly라서 안전하다"가 아니다.** httpOnly는 **JS의 토큰 읽기**(XSS)를 막는 것이고, **기업 간 격리는 서버가 세션 `companyId`로 걸러야** 성립한다. URL에 안 넣는 건 "고칠 수 있는 값을 신뢰하지 않는다"는 설계일 뿐 방어 자체는 **서버 재검사**다(§권한).
- 쿠키는 자동 전송이라 **CSRF**가 따라온다. Server Action은 Next가 Origin을 검사하지만, **BFF 라우트 핸들러(`route.ts`)를 직접 여는 경우엔 직접 챙긴다.**

### 도입할 것

| #                                                | 항목 | 이유                                                                                                             |
| ------------------------------------------------ | ---- | ---------------------------------------------------------------------------------------------------------------- |
| **zod 계약 SSOT + 매퍼 `safeParse`**             | ✅   | ERD·API가 없어 목으로 먼저 간다. shape가 어긋나면 **가짜 데이터가 화면에 뜬 채 모른다** — zod가 연동 전에 터뜨림 |
| **shadcn/ui + cva + cn**                         | ✅   | 폼·테이블·모달·캘린더가 많음. raw div면 3명 스타일이 갈리고 a11y가 샘                                            |
| **CI 필수 체크 4종**                             | ✅   | PR마다 `typecheck·lint·test·build` 전부 통과해야 머지. develop 보호 + 리뷰 1명 + 직접 push 금지                  |
| **공용 목 팩토리(faker ko)**                     | ✅   | 3명이 각자 만들면 shape가 갈림. **1명이 소유**                                                                   |
| **계약 선(先)머지**                              | ✅   | 병렬 착수 전에 `schemas.ts`/`types.ts`만 담은 작은 PR을 먼저 머지 → 3인이 같은 shape 공유                        |
| **husky + lint-staged + Prettier + import 정렬** | ✅   | 커밋 자체가 정리된 상태로만 생성                                                                                 |
| **토스트(shadcn `sonner`)**                      | ✅   | Server Action은 화면 전환 없이 끝나는 변경이 많다. 결과 표시가 없으면 사용자가 같은 버튼을 두 번 누른다          |

#### git 브랜치·커밋 컨벤션 (2026-07-30 확정)

**이슈 번호가 브랜치·커밋·PR을 잇는 고리다.**

| 항목   | 형식                                                                                     | 예                              |
| ------ | ---------------------------------------------------------------------------------------- | ------------------------------- |
| 브랜치 | `feature/{도메인}-{기능}#{이슈번호}` · `fix/{...}#{n}` · `docs/{...}#{n}` (base develop) | `feature/meeting-capture#12`    |
| 커밋   | `type: 제목 #{이슈번호}` (한글 50자)                                                     | `feat: 회의 캡처 녹음 버튼 #12` |
| PR     | 본문에 `Closes #{이슈번호}`                                                              | `Closes #12`                    |

커밋 type 9종(PR 템플릿과 동일): `feat` · `fix` · `style` · `refactor` · `docs` · `chore` · `test` · `design` · `merge`

> 왜 이슈 번호를 브랜치·커밋에까지 박나 — `git log`·브랜치 목록만 봐도 어느 이슈 작업인지 바로 보인다. PR 본문 `Closes`만으로는 머지 후에 추적이 끊긴다.

#### 뷰포트 · 반응형 (2026-07-30 확정)

**1440 기준으로 설계한다. 단 고정 px·absolute를 쓰지 않아 반응형 여지를 남긴다.**
**모바일 대응 화면 선별은 디자인 확정 후 결정한다.**

지금 반응형을 전면 구현하지는 않는다. 캡처(자막↔메모 1:1)·DnD 보드·조직도·고밀도 목록은 좁은 화면에서 **축소가 아니라 재설계**가 필요해서, 화면 확정 전에 손대면 두 번 만들게 된다.

대신 **나중에 붙일 수 없게 되는 것만 막는다** — 비용이 거의 없다.

```tsx
❌ w-[1440px]              ✅ mx-auto max-w-[1440px] px-8
❌ absolute top-[64px]     ✅ flex / grid
❌ <table> 단독             ✅ overflow-x-auto 컨테이너로 감싸기
❌ 사이드바를 layout에 직접  ✅ 컴포넌트로 분리 (모바일은 Sheet로 교체)
```

- 모바일 후보는 **조회·확인형 화면**이다 — 내 액션 목록 · 알림 · 회의 상세 읽기 · 마이페이지 · 결재 승인.
- 캡처·보드·조직도는 데스크톱 유지. 모바일 접근 시 **"PC에서 이용해주세요" 안내를 띄운다** — 깨진 화면을 보여주지 않는다(정직성).

#### 조직 계층(부서 > 역할) · 사원 소속 · 팀장 범위 (2026-07-31 확정)

조직도는 **트리 2계층**이다. 처음엔 이걸 "상위 부서 > 하위 부서"로 읽었는데, 그러면 개발팀 밑에 또 팀이 생기는 꼴이라 실제 조직과 맞지 않았다. 아래 두 규칙으로 못 박는다.

> ⚠️ **기존 `CONVENTIONS §6`의 "부서는 3계층"에서 2계층으로 줄인 것이다.** 기획 확정본과 어긋나면 기획 쪽에 확인이 필요하다.

**① 아랫단은 하위 부서가 아니라 그 부서 안의 _역할_ 이다.**
사원은 **부서에 소속되고**, 역할은 그 안에서 맡는 일이다. 역할은 **비워둘 수 있다**(=`없음`).

```text
개발팀          ← 부서 · 사원이 소속되는 단위
├─ 프론트엔드    ← 역할
└─ 백엔드        ← 역할
디자인팀        ← 역할을 안 나눈 부서 — 이대로도 된다
```

```text
부서: 개발팀 / 역할: 프론트엔드 / 직급: 과장 / 권한: MEMBER
부서: 개발팀 / 역할: 없음      / 직급: 팀장 / 권한: LEADER
```

> ⚠️ **권한은 부서가 아니라 직급에서 온다.** 온보딩 2단계에서 직급마다 LEADER/MEMBER를 정하고,
> 사원은 그 직급을 물려받는다. **LEADER 직급은 회사에 하나**, 그 직급을 가진 사람은 **부서마다 한 명**이다.

**② LEADER는 자기 부서 전체를 관리한다.**
조회·회의 초대·통계·중간승인 전부 **부서 안의 모든 역할 포함**이다. 개발팀 팀장은 프론트엔드·백엔드 사원까지 본다.

이 둘로 아래가 자동으로 결정된다.

| 기능                  | 결과                      |
| --------------------- | ------------------------- |
| 회의 "부서 전체 초대" | 그 부서의 모든 역할 포함  |
| 인수인계 부서 필터    | 역할 무관, 부서 단위      |
| 부서별 통계           | 부서 = 역할 **합산**      |
| 사원 배정 대상        | 부서를 고르고 역할은 선택 |

> ⚠️ **BE 협의 필수 안건.** `departmentId` 하나로는 "내 하위인가"를 판정할 수 없다.
> **응답에 부서 경로(부서→역할, 예: `[1, 5]`)가 필요하다.** 없으면 ②를 구현할 수 없다.
> 나중에 붙이면 응답 형태를 바꿔야 해서 비싸다 — 스펙 협의 때 먼저 꺼낸다.

#### 색 사용 규칙 (2026-07-31 확정)

**포커스 링은 먹색이다**(`--ring` = `#1C1917` / 다크 `#D6D3D1`). 파랑 액센트는 강조가 과했다.

- **색으로 알리는 것은 에러(빨강)뿐이다.** 나머지 상태는 명도·아이콘·문구로 구분한다.
- 초록(`--success`)은 "이미 처리됨" 같은 완료 신호에만 쓰고, 단계 진행 표시처럼 **위치로 알 수 있는 것에는 쓰지 않는다**(온보딩 스텝퍼는 먹색).

#### 다크모드 (2026-07-30 확정)

**적용 범위 = 전 페이지.** `next-themes`로 `<html>`에 `.dark`를 붙이고, 토큰이 통째로 뒤집힌다.

- 컴포넌트는 **토큰만** 쓴다 → 화면별 다크 대응 작업이 0이다. `dark:` 클래스 직접 사용 금지.
- **다크 배경에 순검정을 쓰지 않는다** — 흰 텍스트 halation + 층 소실. `#1A1715`를 가장 어두운 값으로 쓴다.
  ⚠️ 명도 3단(`사이드바 < 배경 < 카드`) 규칙은 **폐기**했다 — 아래 §셸 표면 참고.
- 초기값은 `system`(OS 설정 따라감), 사용자가 토글하면 그 선택을 기억한다.

#### 셸 표면 색 (2026-08-01 확정)

**껍데기(사이드바 · 상단바)는 한 색, 본문은 점 그리드로 구분한다.**

처음엔 `사이드바 < 배경 < 카드` 순으로 명도를 한 단씩 띄웠다. 그런데 실제로 만들어 보니
**사이드바·상단바·본문이 다 달라 화면이 셋으로 조각나 보였다.** 경계를 줄이는 쪽이 정돈돼 보인다.

| 자리              | 토큰                            | 라이트 / 다크         |
| ----------------- | ------------------------------- | --------------------- |
| 사이드바 · 상단바 | `--background`                  | `#FFFFFF` / `#1A1715` |
| 본문              | `--background` + `.bg-dot-grid` | 같은 색 + 점          |
| 카드              | `--card`                        | `#FFFFFF` / `#242120` |

- 껍데기와 본문은 **색이 아니라 점 그리드**로 나뉜다. 선은 사이드바 오른쪽·상단바 아래 하나씩만.
- **사이드바 안에서 또 나누지 않는다** — 로고 아래 가로선을 두지 않는다.
- 점은 토큰(`--border`)으로 그린다. `.bg-dot-grid` 유틸 하나를 온보딩과 로그인 뒤 화면이 같이 쓴다.
- ⚠️ 순검정 금지는 그대로다.

#### 결과 알림 모달 (2026-08-01 확정)

**"다 됐어요" 류 모달은 `components/common/success-dialog.tsx` 하나를 쓴다.**

화면마다 원·정렬·여백을 새로 잡으면 모양이 갈린다. 컴포넌트가 표시·정렬·여백을 잡고,
화면은 **제목 · 설명 · 요약 · 버튼**만 채운다. 온보딩 완료 화면과 같은 결이다(먹색 원 + Z 로고 + 체크 배지).

- ⚠️ **확인을 받는 창**(삭제할까요?)에는 쓰지 않는다 — 선택지가 둘이라 다른 모양이어야 한다.
- ⚠️ 초록·파랑을 쓰지 않는다(§색 사용 규칙).

#### CODEOWNERS — 문서도 3인 공동 (2026-08-01 확정)

**문서(`*.md` · `/docs/**`)의 소유자를 한 명에서 프론트 3인 공동으로 바꿨다.**

⚠️ **자기 PR은 자기가 승인할 수 없다.** 소유자가 한 명인데 그 사람이 곧 작성자면
승인할 사람이 아무도 남지 않아 **영구 교착**이 된다. 실제로 문서 PR이 막혔다.

문서도 결국 팀 기준이므로 `/src/**` 와 같은 규칙으로 둔다 — 셋 중 한 명이 보면 된다.

#### develop 룰셋 — strict 해제 (2026-08-01 확정)

**`strict_required_status_checks_policy`를 껐다.**

PR 하나가 머지될 때마다 나머지가 전부 `BEHIND`가 됐다. 브랜치 업데이트 → CI 재실행 →
`dismiss_stale_reviews_on_push`로 **승인까지 날아가는 것**이 반복됐다. PR이 3개면 3번이다.

- **남긴 것:** `verify` 필수 체크 · 승인 1명 · 코드오너 리뷰 · 푸시 시 기존 승인 해제
- 껐다고 깨진 코드가 들어오지는 않는다 — `verify`는 여전히 통과해야 한다.
- ⚠️ 팀이 커지거나 develop 충돌이 잦아지면 다시 켜는 걸 검토한다.

#### 토스트 — 결과 피드백 (2026-07-30 확정)

**Server Action의 성공·실패는 토스트로 알린다.** shadcn `sonner`(`next-themes` 이미 있어 다크모드는 따라온다).

| 토스트로 알린다                                              | 토스트로 하지 않는다                                             |
| ------------------------------------------------------------ | ---------------------------------------------------------------- |
| 액션 상태 변경 · 결재 승인/반려 · 초대 발송 등 **변경 결과** | **폼 검증 오류** → 어느 필드인지 알려줘야 하므로 **필드 인라인** |
| 되돌리기(Undo)를 줄 수 있는 변경                             | **파괴적 작업 확인** → `Dialog` (토스트는 확인 수단이 아니다)    |
| SSE 알림 도착                                                | **페이지 전체 실패·빈 상태** → `error.tsx` · empty 화면          |

- `<Toaster />`는 `app/layout.tsx`에 **1개**. 공유 셸이라 ③이 단독으로 넣는다(§분업).
- ⚠️ **토스트만으로 정보를 전달하지 않는다.** 토스트는 사라지므로, 결과는 `revalidatePath` 후 화면에 반영되어 있어야 하고 토스트는 보조다. 자리를 비웠거나 스크린리더를 쓰면 놓친다.
- 문구는 **~합니다체**(2026-08-04 변경). 실패 토스트는 **원인과 다음 행동**까지 — "실패했습니다"만 띄우지 않는다.
- 설치·배치 완료(2026-07-31) — `sonner` 패키지 + `components/ui/sonner.tsx`(모양·색) + 루트 레이아웃의 `<Toaster />`. 화면에서는 `toast.success(...)`만 부른다.
- 모양은 **한 곳(`components/ui/sonner.tsx`)에서만** 고친다 — 화면 최상단 가운데 · 먹색 · 둥근 모서리 · 한 줄.

### 도입 안 할 것

| 항목                                       | 왜 안 하나                           | 대신                                                                                                                                                         |
| ------------------------------------------ | ------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **plop 스캐폴딩 + 골든 레퍼런스**          | 초기 셋업 비용 대비 3인·1개월엔 과함 | ⚠️ **구조 일관성은 `CONVENTIONS.md §2`와 리뷰로만 지킨다.** 첫 도메인을 만들 때 폴더 4파일(`schemas·types·server·actions`) 형태를 지키는지 리뷰에서 꼭 볼 것 |
| **서버상태 라이브러리(TanStack Query 등)** | Server-First라 조회는 서버가 함      | 클라 상태는 `useState` + Server Action. 정말 부족하면 그때 재논의                                                                                            |
| **commitlint 하드 블록**                   | 마찰만 큼                            | 커밋 규칙(`type: 제목 #{이슈번호}`)은 **유지**하되 훅으로 막지 않고 **PR 리뷰에서** 본다                                                                     |
| **`/preview` 컴포넌트 카탈로그 확장**      | 화면 만들기가 우선                   | `/preview` 라우트 자체는 화면 목록에 있으니 그대로 두고, 상태 카탈로그는 안 만듦                                                                             |

---

## ⏳ 미결정 (정해지면 이 문서부터 갱신)

### 프론트가 정할 것

- [x] **화면·워크플로우 명세 확정** (2026-07-30) — 명세는 **팀 문서에 두고, 화면별 상세는 이슈 본문으로** 옮긴다. 레포 문서에는 잘 바뀌지 않는 규칙만 남긴다
- [ ] **디자인 시안 확정** — 화면 목록은 정해졌지만 시안은 아직. 시안 없이 픽셀을 지어내지 않는다
- [ ] **①②③ 담당자 배정** — 분할 기준은 확정, 누가 어디를 맡을지 미정
- [ ] **CODEOWNERS** — 담당자 배정 + 팀원 GitHub 아이디가 정해지면 `.github/CODEOWNERS`로 만든다. 초안:

  ```
  /src/app/(app)/meeting/**  /src/features/meeting/**                    @devA   # ① 워크벤치
  /src/app/(app)/board/**    /src/features/{action,project,handover}/**  @devB   # ② 업무
  /src/app/{(role),(shell)/(shared)}/**  /src/features/{member,room,billing}/** @devC  # ③ 조직
  /src/app/layout.tsx  /src/app/globals.css  /src/components/ui/**       @devC   # 공유 셸 단독
  /src/**/schemas.ts   /src/types/**                    @devA @devB @devC        # 계약 = 공동 소유
  ```

  ⚠️ 강제력은 GitHub Settings → Branches에서 `develop`에 **"Require review from Code Owners"** 를 켜야 생긴다.

### 기획·BE 대기

| 항목                             | 막고 있는 것                                                    |
| -------------------------------- | --------------------------------------------------------------- |
| **ERD · API 스펙**               | 실연동 (⚠️ **목으로는 진행 가능** — 격리막 + zod가 교체를 감당) |
| AI 실모델/API                    | 데모는 목                                                       |
| 결제 실연동(Toss)                | 구독 화면 마감                                                  |
| **배포 — AWS 확정, 서비스 미정** | CI 마지막 단계 (아래 참고)                                      |
| 온라인 회의 반영 시점            | 회의 도메인 범위                                                |

#### 퇴사 표기 — 흐름은 "오프보딩", 상태는 "퇴사" (2026-08-05 확정)

한 낱말로 통일하려다 오래 걸렸는데, **두 곳이 다른 것을 가리키고 있었다.**

|               | 값                          | 화면 라벨 | 무엇을 가리키나                                         |
| ------------- | --------------------------- | --------- | ------------------------------------------------------- |
| 인수인계 유형 | `HANDOVER_TYPE.OFFBOARDING` | 오프보딩  | **신청·승인 흐름** — 액션을 재분배하고 결재를 타는 과정 |
| 회원 상태     | `MEMBER_STATUS.RESIGNED`    | 퇴사      | **끝난 뒤의 사람 상태**                                 |

⚠️ **나간 사람은 목록에서 지우지 않는다.** 그 사람이 남긴 회의·액션·인수인계의 출처라,
이름이 사라지면 그 기록들이 어디서 왔는지 알 수 없게 된다. Z의 추적 순환이 사람 이름에
걸려 있어서다.

⚠️ **`RESIGNED`와 `DELETED`는 다르다.** `DELETED`는 BE의 소프트 딜리트 값이고 **상태가 아니다** —
상태 목록에 넣으면 `재직·휴직·대기·퇴사` 필터에 다섯째 칸이 생긴다. 내려오면 매퍼가 거른다.

#### 배포 — AWS (2026-07-30 방향 확정 · 서비스 미정)

**Vercel이 아니라 AWS로 간다.** 어느 서비스를 쓸지는 미정.

> ⚠️ **정적 배포(S3 + CloudFront 단독)는 후보가 아니다.** Server Action · BFF 프록시 · httpOnly 쿠키 · SSE 알림이 모두 **Node 서버가 살아 있어야** 동작한다. `next export`로는 이 구조가 성립하지 않는다.

| 후보                | 특징                                                                                                               |
| ------------------- | ------------------------------------------------------------------------------------------------------------------ |
| **Amplify Hosting** | Next.js SSR·Server Action 공식 지원. GitHub 연동 자동 배포 + PR 미리보기 URL. 설정 부담이 가장 적다                |
| ECS / Fargate       | Dockerfile로 컨테이너화. 통제권이 크고 BE와 방식을 통일할 수 있으나 ECR·로드밸런서·파이프라인을 직접 구성해야 한다 |
| EC2 직접            | 단순해 보이지만 배포 자동화·재시작·HTTPS를 손으로 관리해야 한다                                                    |

- 컨테이너(ECS·EC2)로 간다면 `next.config.ts`에 **`output: 'standalone'`** 이 필요하다. Amplify는 불필요.
- 결정 시점: 화면이 몇 개 나와서 실제로 올려볼 게 생겼을 때. 지금 미리 정할 필요는 없다.

---

> **ERD가 없어도 착수한다.** 격리막(`CONVENTIONS §19`) 덕분에 나중에 `server.ts`·매퍼만 고치면 컴포넌트는 안 건드린다. 그래서 **zod가 선택이 아니라 전제**다.
