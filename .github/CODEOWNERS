# 코드 소유자 — 여기 걸린 파일이 바뀐 PR은 해당 소유자의 리뷰가 있어야 머지된다.
#
# ⚠️ 강제력은 GitHub → Settings → Rules(Rulesets)의 develop 룰셋에서
#    "Require review from Code Owners"를 켜야 생긴다. 켜기 전까지는 리뷰어 자동 지정까지만 동작한다.
# ⚠️ 협업자 전원이 admin이므로, 룰셋의 bypass list에는 "Repository admin" 역할이 아니라
#    특정 사용자(@hyun030514)만 넣는다. 역할로 넣으면 전원이 룰셋을 통과한다.
#
# 규칙: 마지막에 매칭된 줄이 이긴다. 소유자는 저장소 write 권한이 있어야 인식된다.

# ── 프론트엔드 코드 — 프론트 3인 중 1명의 리뷰가 필요하다.
# 백엔드·기획 인원의 승인만으로 프론트 코드가 머지되는 것을 막는다.
/src/**                 @hyun030514 @zana0422-creator @CallmeEterHist

# ── 문서 — 규칙·결정 기록이라 아무나 고치면 기준이 흔들린다.
# 큰 변동이 아니면 문서는 건드리지 않는다. 고쳐야 하면 이 리뷰를 거친다.
*.md                    @hyun030514
/docs/**                @hyun030514

# ── 프로젝트 설정 — 전원에게 영향이 가지만, 한 명만 소유하면 병목이 된다.
# 라이브러리 추가 하나로 PR이 멈추지 않도록 프론트 3인 공동 소유로 둔다.
/.github/**             @hyun030514 @zana0422-creator @CallmeEterHist
/package.json           @hyun030514 @zana0422-creator @CallmeEterHist
/package-lock.json      @hyun030514 @zana0422-creator @CallmeEterHist
/tsconfig.json          @hyun030514 @zana0422-creator @CallmeEterHist
/eslint.config.mjs      @hyun030514 @zana0422-creator @CallmeEterHist
/next.config.ts         @hyun030514 @zana0422-creator @CallmeEterHist

# ── TODO(①②③ 담당 배정 후): 아래를 켜서 화면 구역별 소유자를 나눈다.
#    지금은 셋 다 /src/** 공동 소유이므로 아무나 한 명이 승인하면 머지된다.
# /src/app/(app)/meeting/**  /src/features/meeting/**                    @???   # ① 워크벤치
# /src/app/(app)/board/**    /src/features/{action,project,handover}/**  @???   # ② 업무
# /src/app/(role)/**         /src/features/{member,room,billing}/**      @???   # ③ 조직
# /src/app/layout.tsx  /src/app/globals.css  /src/components/ui/**       @???   # 공유 셸 단독 소유
